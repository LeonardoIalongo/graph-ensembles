Network ensembles
=================


Development
-----------
To build a development environment run::

    python3 -m venv venv 
    source venv/bin/activate 
    pip install -e '.[dev]'

For testing::

    pytest` or `pytest --cov
